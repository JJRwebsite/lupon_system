# SMTP configuration for email verification (Gmail example)
EMAIL_FROM="Lupon Barangay Office <your_gmail@gmail.com>"
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=your_gmail@gmail.com
SMTP_PASS=your_app_password

# If Gmail 465 doesn’t work in your network, try STARTTLS on 587
# SMTP_PORT=587
# SMTP_SECURE=false

# Existing envs you may already have
NODE_ENV=development
PORT=5000
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=lupon_db
